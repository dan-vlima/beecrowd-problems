const lines = []

const calculateVolume = (lines) => {
   const pi = 3.14159;
   const r = lines;
   volume = (4/3.0) * pi * (r * r * r);
};

calculateVolume(3);

console.log(`VOLUME = ${volume.toFixed(3)}`);